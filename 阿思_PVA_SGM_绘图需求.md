# 🎨 绘图需求：PVA v2 SGM 立体匹配流程图

> **绘制给**：阿思
> **用途**：MotionLabs README / 技术文档用 HTML 交互式架构图
> **风格**：深色科技风（参考 NVIDIA 开发者文档风格），带发光线条
> **输出格式**：HTML（单文件，自包含，可嵌入 README）

---

## 一、总览

绘制一张 **双目立体视觉 → PVA SGM 匹配 → 输出深度图** 的垂直流程图，展示从已校正 L/R 图像到深度图的完整数据流，同时标注硬件边界（VIC / PVA / GPU / DLA）和并行关系。

### 核心主题

**对已校正 L/R 全图做 SGM 立体匹配 → 输出视差图**
- PVA 专为 SGM 设计
- 与 GPU 可并行运行

---

## 二、布局结构（从上到下）

### 整体画布

- **尺寸**：宽 1200px，高 1600px
- **背景**：深色（#0D1117 或 #1A1A2E）
- **字体**：系统无衬线（"SF Pro Display", -apple-system, sans-serif）
- **主标题**：顶部居中 "⚡️ PVA v2 SGM 立体匹配流程"
- **副标题**：".3ms 亚像素精度 · 与 GPU 并行运行"

### 第一层：输入

**两个并排的矩形框**，水平排列：

```
┌─────────────────────┐  ┌─────────────────────┐
│  Left Rectified      │  │  Right Rectified     │
│  (已校正左图)         │  │  (已校正右图)         │
│                     │  │                     │
│  640×480 · U8       │  │  640×480 · U8       │
│  NVMM memory        │  │  NVMM memory        │
└─────────────────────┘  └─────────────────────┘
```

- 颜色：深蓝渐变 (#1E3A5F → #0D2137)
- 边框：蓝色发光 (#4FC3F7)
- 下方标注 "来自 VIC Remap 硬件矫正 · 极线对齐"

### 第二层：核心处理 — PVA SGM 流水线

**中央大框**，内部包含 6 个步骤的水平流水线：

```
┌──────────────────────────────────────────────────────────┐
│  ⚡️ PVA 可编程视觉加速器 (Programmable Vision Accelerator v2) │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐ │
│  │Census │→ │Cost  │→ │ SGM  │→ │WTA   │→ │Sub-  │→ │L-R   │ │
│  │Transform│  │Comp. │  │Aggre-│  │Winner│  │pixel │  │Consis│ │
│  │       │  │      │  │gation│  │Takes │  │Interp│  │tency │ │
│  │ 5×5/7×7│  │      │  │4-8 dir│  │All   │  │×1/8px│  │Check │ │
│  └──────┘  └──────┘  └──────┘  └──────┘  └──────┘  └──────┘ │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  Median Filter (3×3 中值滤波)                        │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────┘
```

- PVA 大框：紫色渐变 (#6A1B9A → #4A148C)，边框紫色发光 (#CE93D8)
- 内部 6 个步骤：深紫 (#1A0033)，边框浅紫 (#9C27B0)
- 步骤之间用箭头连接 (→)

#### 每个步骤的详细说明（用 tooltip 或小字标注在下方）

1. **Census Transform**
   - 输入：U8 灰度图像
   - 输出：Census 编码图像
   - 描述：对每个像素的 5×5 或 7×7 邻域做 Census 变换，生成局部结构描述子（bit string）
   - 公式：`C(p) = ⊗_{q∈N(p)} ξ(I(p), I(q))`，其中 ξ(x,y)=1 if x<y else 0

2. **Cost Computation**
   - 描述：计算左右图 Census 编码的 Hamming distance
   - 公式：`Cost(d) = Hamming(C_left(p), C_right(p-d))`
   - 搜索范围：d ∈ [0, maxDisparity)

3. **SGM Aggregation** ← 核心（高亮）
   - 描述：沿 4 或 8 个方向聚合代价
   - 公式：`L_r(p,d) = Cost(p,d) + min(L_r(p-r,d), L_r(p-r,d±1)+P1, min_k(L_r(p-r,k))+P2) - min_k(L_r(p-r,k))`
   - P1=8, P2=32（可调）
   - 标注："Semi-Global Matching 核心 — 多方向DP"

4. **Winner-Takes-All**
   - 描述：每个像素取最小聚合代价对应的视差
   - 公式：`d*(p) = argmin_d S(p,d)`
   - 输出：整像素精度视差图 S16

5. **Sub-pixel Interpolation** ← 亚像素精度
   - 描述：对 WTA 结果做抛物线/等角拟合
   - 公式：`d_sub = d* + (C(d*-1)-C(d*+1)) / (2*(C(d*-1)-2*C(d*)+C(d*+1)))`
   - 标注：**1/8 像素精度**（×8 定点：value/8 = 实际像素）
   - 高亮：★ 这是"亚像素精度"的关键

6. **L-R Consistency Check**
   - 描述：左右一致性检查，标记遮挡点
   - 阈值：|dL - dR| ≤ 1 pixel
   - 无效点标记为 0

7. **Median Filter**（在流水线末尾）
   - 描述：3×3 中值滤波去噪
   - 可选：5×5

### 第三层：输出

**两个并排的矩形框**：

```
┌─────────────────────┐  ┌─────────────────────┐
│  Disparity Map       │  │  Depth Map           │
│  (视差图)             │  │  (深度图)             │
│                     │  │                     │
│  S16 · ×8 亚像素     │  │  F32 · meters       │
│  640×480             │  │  640×480            │
│  NVMM memory         │  │  NVMM memory        │
└─────────────────────┘  └─────────────────────┘
```

- 视差图框：橙色渐变 (#E65100 → #BF360C)
- 深度图框：绿色渐变 (#1B5E20 → #003300)
- 中间用箭头 `→` 连接，标注 "depth = f·b / d"

---

## 三、硬件并行关系图（侧边/底部单独区域）

在流程图右侧或底部画一个 **并行时间轴**（类似 Gantt 图）：

```
时间 → t0          t+1ms       t+3ms       t+5ms       t+8ms
       ┌──────────┐
VIC    │ Remap    │
       └──────────┘
                  ┌──────────┐
PVA    │ Idle    │  SGM Stereo     │
                  └────────────────┘
       ┌─────────────────────────────┐
DLA-0  │     YOLO 26n Left           │
       └─────────────────────────────┘
       ┌─────────────────────────────┐
DLA-1  │     YOLO 26n Right          │
       └─────────────────────────────┘
                                           ┌─────────────────────┐
GPU    │  Idle (等待 fence)               │  ViTPose + SMPL      │
                                           └─────────────────────┘
```

- VIC: 蓝 (#2196F3)
- PVA: 紫 (#9C27B0)
- DLA: 橙 (#FF9800)
- GPU: 绿 (#4CAF50)
- 关键标注："fence sync" 在 VIC→PVA 和 PVA+DLA→GPU 的连接处

---

## 四、性能标签

在 PVA 框右下角放一个性能总结标签：

```
╔══════════════════════════════════╗
║       ⚡️ PVA SGM 性能           ║
╠══════════════════════════════════╣
║ 分辨率    640×480   1280×720     ║
║ 延迟      ~3-5ms    ~8-12ms      ║
║ 功耗      0.5-1W    ~1.5W        ║
║ 亚像素    ×8 (1/8 px)           ║
║ 最大视差  64 (可调至 128)        ║
║ GPU 占用  ❌ 不占用              ║
╚══════════════════════════════════╝
```

---

## 五、零拷贝数据流标注

在图的左侧画一条垂直的标注线，表示数据全程在 NVMM 中，无 CPU 拷贝：

```
┌──────────────────────────────────────┐
│  🔄 零拷贝 (Zero-Copy) 数据流       │
│                                      │
│  VIC Remap ──NVMM──→ PVA SGM        │
│                                    │
│  PVA SGM ──NVMM──→ GPU 3D Recon    │
│                                      │
│  ✅ 全程无 CPU 介入                  │
│  ✅ NvBufSurface fence 同步          │
└──────────────────────────────────────┘
```

位置：整个流程图的左侧，竖排

---

## 六、颜色方案

| 元素 | 背景色 | 边框/发光 | 文字色 |
|------|--------|-----------|--------|
| 画布背景 | #0D1117 | — | — |
| 主标题 | — | — | #FFFFFF |
| 输入图像框 | #1E3A5F → #0D2137 | #4FC3F7 | #E0E0E0 |
| PVA 大框 | #1A0033 | #CE93D8 | #E1BEE7 |
| PVA 内部步骤 | #2A0053 | #9C27B0 | #FFFFFF |
| SGM 核心高亮 | #4A148C | #E040FB 脉冲 | #FFFFFF |
| 亚像素高亮 | #FF6F00 | #FFD54F | #000000 |
| 输出视差图 | #E65100 → #BF360C | #FF9800 | #FFFFFF |
| 输出深度图 | #1B5E20 → #003300 | #4CAF50 | #FFFFFF |
| 性能标签 | #1A237E | #448AFF | #BBDEFB |
| 并行时间轴 VIC | #2196F3 | — | 白色 |
| 并行时间轴 PVA | #9C27B0 | — | 白色 |
| 并行时间轴 DLA | #FF9800 | — | 白色 |
| 并行时间轴 GPU | #4CAF50 | — | 白色 |
| 零拷贝标注 | #00695C | #26A69A | #B2DFDB |
| 箭头 | — | #64B5F6 渐变 | — |

---

## 七、动效需求（可选，HTML 交互）

1. **数据流动画**：PVA 内部 6 个步骤之间有流动的光点从左到右移动
2. **脉冲高亮**：SGM Aggregation 和 Sub-pixel Interpolation 两个关键步骤有脉冲发光效果
3. **悬挂标签**：鼠标悬停在每个步骤上时，弹出详细说明浮层（tooltip）
4. **时间轴**：有一定的展开/折叠交互
5. **粒子背景**：深色背景中稀疏的发光粒子缓慢移动（科技感）

---

## 八、文字标注清单

以下文字需要精确出现在图中：

1. 主标题：`⚡️ PVA v2 — 可编程视觉加速器  SGM 立体匹配`
2. 副标题：`~3ms · 亚像素精度 · 与 GPU 并行运行`
3. 输入说明：`已校正 L/R 全图 · 来自 VIC Remap 硬件矫正`
4. 步骤 1：`Census Transform · 5×5/7×7 局部结构描述子`
5. 步骤 2：`Cost Computation · Hamming Distance`
6. 步骤 3：`SGM Aggregation · 多方向 DP 代价聚合` ← 核心高亮
7. 步骤 4：`Winner-Takes-All · 取最小代价视差`
8. 步骤 5：`Sub-pixel Interpolation · 抛物线拟合 · 1/8 px 精度` ← 高亮
9. 步骤 6：`L-R Consistency Check · 遮挡过滤`
10. 后处理：`Median Filter · 3×3 中值去噪`
11. 输出 1：`S16 Disparity Map · ×8 亚像素定点`
12. 输出 2：`F32 Depth Map · depth = f·b / d`
13. 性能标签内容（见第四章）
14. 零拷贝标注：`全程 NVMM · 无 CPU 拷贝 · fence 同步`

---

## 九、参考图风格

参考 NVIDIA VPI 文档风格（https://docs.nvidia.com/vpi/）的深色科技风：
- 类似 NVIDIA Developer 图示的暗紫/深蓝配色
- 线条有发光效果（drop-shadow / glow）
- 硬件模块用带渐变边框的圆角矩形
- 数据流用带箭头的粗线

---

以上是绘图需要的全部信息。阿思可以直接按此需求绘制 HTML。
